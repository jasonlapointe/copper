<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CopperBot</RootNamespace>
    <AssemblyName>copper-bot</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <!-- Application Default Credentials for Vertex AI: metadata server on Cloud Run, ADC locally. -->
    <PackageReference Include="Google.Apis.Auth" Version="1.69.0" />
    <!-- Durable persona storage so the people-network survives Cloud Run scale-to-zero. -->
    <PackageReference Include="Google.Cloud.Firestore" Version="3.9.0" />
  </ItemGroup>

</Project>
