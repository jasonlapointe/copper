# Copper — Decisions

Settled. Don't relitigate without new evidence.

| # | Date | Decision | Why |
|---|------|----------|-----|
| 1 | 2026-09-21 | **Name: Copper.** C# / .NET 9 console app. | Jason's call. |
| 2 | 2026-09-21 | **Chat agent, not a command CLI.** Plain-English in, agent decides which tools to run. | Jason: "must look like a chat agent"; the product IS the bridging intelligence. |
| 3 | 2026-09-21 | **WhatsApp access via existing Node bridge (`wa.js` / whatsapp-web.js), spawned per call.** Not a C# WhatsApp client, not a rewrite. | No official personal-WhatsApp API; the protocol is E2E-encrypted and complex. Bridge already works and is patched. Revisit only if the library breaks again (fallback: Baileys). |
| 4 | 2026-09-21 | **whatsapp-web.js pinned at 1.34.7 + local reviewed patch** (`../patches/wwebjs-fix.diff`) for the WA Web 2.3000.104x `_serialized`→`$1` rename. Re-apply after every `npm install`. Never install unreviewed forks. | Library has full session access — supply-chain risk. Patch was read line-by-line before applying. |
| 5 | 2026-09-21 | **Brain: Claude API, model `claude-opus-5`, manual tool-use loop** (non-beta Messages API). | Opus 5 is the current default recommendation; manual loop is fully documented in C# and we need custom control at the send gate anyway. |
| 6 | 2026-09-21 | **Send gate is code, not prompt:** the `send_whatsapp` tool prints text + back-translation and blocks on console `y/N`. | Prompt-level rules can drift; an in-code gate cannot. Invariant. |
| 7 | 2026-09-21 | **Reading is passive — no read receipts** (`sendSeen` removed from bridge). | A go-between shouldn't signal "read" before Jason has actually read. |
| 8 | 2026-09-21 | **Working language with Marat: Russian** (match what he writes), Kazakh greeting flourishes OK. Follow him if he switches. | Matching the other person's chosen language is the natural, zero-risk default. Jason approved the recommendation. |
| 9 | 2026-09-21 | **Voice: Jason's persona** — technical, detail-oriented, to the point. Compact natural phrasing, culturally required beats kept, no toastmaster flourish. | Jason: "honor my persona"; an effusive message in his name misrepresents him. |
| 10 | 2026-09-21 | **Long-term memory = markdown profile per contact** (`profiles/<name>.md`), seeded by hand, appended by the agent via `update_contact_memory`. Loaded whole into the system prompt. | Simplest thing that makes turn 10 better than turn 1. Human-readable/editable. Revisit if profiles get big. |
| 11 | 2026-09-21 | **One contact per Copper config** (`copper.json`); Jason forks per relative. Marat first. | Mirrors his conversation-fork workflow; avoids contact-switching UX before the concept is proven. |
| 12 | 2026-09-21 | **MVP before architecture.** No GUI, no polling/notifications, no media download, no multi-contact, no packaging until a real Marat exchange validates the concept. | Jason: "don't over-architect before we prove the concept." |
| 13 | 2026-09-21 | **In-session conversation history is ephemeral;** durable state = WhatsApp's own chat history + the profile file. | Keeps MVP simple; the transcript already lives in WhatsApp. |

| 15 | 2026-09-21 | **Mission: know the person, not just the language.** The contact model is Copper's core asset. Sources, in value order: the conversation itself → asking Jason → materials Jason brings (LinkedIn, photos, backstory). Copper elicits actively when a gap matters. | Jason: "we MUST learn about the person on the other end... that context is the difference between a translation engine and a real interaction." |
| 16 | 2026-09-21 | **Copper doesn't scrape private individuals on its own.** Jason supplies or approves external material about the contact; the richest data is the chat + Jason anyway. | Marat is a private person and family; Jason drives what enters the model. Also practical: LinkedIn blocks scraping. |
| 14 | 2026-09-21 | **Every turn is logged to `logs/turns.jsonl`** — timestamp, user text, reply, tools run, sends/declines, `learned` (profile notes saved), API calls, token counts (incl. cache), duration. | Jason: monitor usage. JSONL is grep/analyze-friendly; no dashboard until the data says we need one. |
| 17 | 2026-09-21 | **People network: `people/*.md`, one file per person, both sides modeled** (jason, marat, sabira, gulmira, + anyone who earns a file). Whole network loads into the system prompt; `update_person` writes to anyone's file, creating new people on first mention. Supersedes the single contact-profile + persona-string design (rows 9–10 stand for voice/content; storage generalized). Runtime stays one active contact per session (row 11). | Jason: "learn about both sides… build a framework of people and interactions that supports a network of communication." Revisit full-network loading if files grow large. |

| 18 | 2026-09-21 | **Dignity principle:** everything Copper renders, in either direction, must read as a fluent, intelligent native being fully themselves — never word-for-word, never foreign-sounding. In the spec's mission and the agent's core rules. | Jason: polyglots get dismissed over accent/word-for-word choices; Copper is the social glue that fixes that. |

| 19 | 2026-09-21 | **Core values locked in VALUES.md** — harmony ("harmonizing us all"), dignity, fidelity, knowing over rendering, consent & trust — ranked, and they outrank features and deadlines. Mirrored in the agent's system prompt. | Jason: "Lock our core values. I believe in harmonizing us all." |

| 20 | 2026-09-21 | **Brain = `claude` CLI headless** (`-p --output-format json`, continuity via `--resume <session-id>`), using this machine's Claude Code login — subscription auth, **no API key**. Anthropic SDK dependency removed; the model answers a strict JSON contract (reply / action / draft / learned) and Copper's code runs the loop. Supersedes the SDK half of row 5 (model stays `claude-opus-5`-class via CLI default). | Jason: "use this session instead of api key" — and he's taken this pattern to production repeatedly. |
| 21 | 2026-09-21 | **One persistent bridge process:** `wa.js serve` speaks JSON-lines over stdio and handles reads, sends, status AND the live feed. Per-command wa.js processes are gone. | Chromium locks the WhatsApp profile (two processes can't coexist), and per-command boot cost ~20s. One process = live feed + fast commands. |
| 22 | 2026-09-21 | **Live feed MVP:** incoming messages print immediately (📩) and fold into the next turn's prompt for translation. No autonomous agent turns / auto-replies yet. | Jason: "connect to a live feed. We are MVP." Push without giving the agent unattended agency. |

| 23 | 2026-09-21 | **UX = local web chat app** at `http://localhost:5077` (ASP.NET minimal API + one static page): chat bubbles, live 📩 incoming, draft card with Send/Cancel buttons as the send gate, SSE for events. Supersedes the console REPL and row 12's GUI deferral. Typing instead of pressing Send/Cancel counts as decline-with-feedback. | Jason: "cmon give me a ux." The gate stays in code — the buttons call the same in-code approval path. |

| 24 | 2026-09-21 | **WhatsApp-style UI with zero visible friction:** left nav lists real chats (display-only; only the active contact is bridged — supersedes nothing, row 11 stands), history preloads, images download and render inline (`wa.js media/` cache → `/api/media/`), and every incoming message is **auto-translated in place** — typing dots while rendering, English as the message, original + cultural note behind a tap. Contract gains a `translations` field; agent calls are serialized (one brain session). Failed translation falls back to the original text. | Jason: "why aren't Marat's messages loaded", "get the images", "I don't want to feel like it is a translation. I don't want to see that friction." |
| 25 | 2026-09-21 | **Truth in presence:** the header says "bridge connected", never "online" — Copper doesn't know the contact's presence and won't fake it. Real presence detection is deferred (library signals unreliable). | Jason caught "online" implying Marat was online. A WhatsApp-skin UI must not lie about what it knows. |

| 26 | 2026-09-21 | **Deterministic pipeline; LLM only at the edges.** Polling, history, live feed, media, and sending are pure C# code — no model in the loop, no agent session, no `--resume`. The LLM is exactly two stateless single-shot calls with code-assembled context (people network + last 15 messages): `TranslateAsync` (incoming → contextual English) and `DraftAsync` (Jason's English → outgoing + back-translation). Confirmed sends are code-only. Supersedes the agent-loop halves of rows 20/23 (session auth via `claude -p` stands). | Jason: "we need a deterministic pipeline not an LLM for polling messages etc. The LLM is only for live translations with context." Also faster and cheaper: 1 model call per direction instead of 2–4. |

| 27 | 2026-09-21 | **Tension-aware interpreting.** Context = stable anchor (people files: who they ARE) + recent transcript (what's happening), and the prompt says which wins: a heated hour never redefines the relationship. Rules: intensity in = intensity out exactly (muting anger is as unfaithful as inflaming it); ambiguity resolves to the less inflammatory reading with a note that the harsher one exists; no editorializing or side-taking; anything that will land harsher/softer than the author intends gets flagged in a note — drafts now carry an optional `note` shown ⚠ in the draft card, so Jason decides with eyes open. | Jason: "context that favors recency? what if we get in a fight" — recency-weighted context would otherwise make the interpreter an escalation amplifier. |

| 28 | 2026-09-21 | **Grounding statements: every person file opens with a dated "who they are RIGHT NOW" snapshot** (3–6 sentences; days/weeks foregrounded, months as background, stale details dropped). The append-only Learned notes stay as the raw log; the grounding is the distillation. Refresh is deterministic-trigger, LLM-prose: on startup, any grounding older than 7 days gets one rewrite from the file + recent chat. All four people seeded 2026-09-21. | Jason: "need a solid grounding statement for each person that captures enough overall recency… a child that grows into an adult — but days, months." People change; an interpreter anchored to a stale portrait misreads the present. |

| 29 | 2026-09-21 | **Grounding refresh has two deterministic triggers:** age (>7 days) and activity (≥10 logged events since the grounding's date, for the contact and Jason) — checked at startup and after each confirmed send. "The last X turns tweak existing knowledge." | Jason: "a persona per person that takes last X turns to perhaps tweak existing knowledge." Time alone misses an eventful evening; volume alone misses slow drift. |
| 30 | 2026-09-21 | **Incoming pictures are analyzed for context.** The CLI brain runs in the bridge dir and READS downloaded image files during translation calls: each photo gets a contextual English caption in the chat (who/where/what), and durable visual facts (places visited, people, objects like the dombra) flow into `learned` → the people network. | Jason: "if you see pictures analyze them for context. Pictures of Marat or where he went or whatever." Photos carry half the family context. |

| 31 | 2026-09-21 | **Natural seeding through the same input box.** The single per-input LLM call now classifies intent: message → draft (gated as always); seeding/guidance ("btw…", "fyi…", third-person facts) → `learned` entries to the right person's file + a 📝 acknowledgment chip showing exactly what was recorded. **Ambiguity defaults to draft** — a wrong draft dies at Cancel; a silently mis-filed note corrupts memory. Wrong ack? Jason says "no, send that to him." | Jason: "I need to be able to feed seeding information for each contact naturally when it occurs to me." No syntax, no separate mode — just talk. |

| 32 | 2026-09-22 | **Seamless send — the approval gate is removed.** Type English + Enter = sent (Jason's Enter IS the approval, like any chat app). His bubble shows his ENGLISH with ✓✓; the Russian Marat received sits behind an explicit control. Supersedes decisions #6 and #23's draft-card gate. | Jason: "be SEAMLESS. How can I possibly judge the translation" — asking him to approve Russian he can't read was fake friction. Trust comes from the persona system + on-demand verify, not a rubber-stamp. |
| 33 | 2026-09-22 | **Explicit labeled reveal, not a vague tap.** Every message carries a clear control: on Jason's sent bubbles "🌐 View in Marat's language" (what Marat received); on Marat's bubbles "🌐 View original (Marat's language)". Both toggle to Hide. The English is always the face; the native-language version is the consumer-side view, one labeled click away. | Jason: "tap is non-specific. Give me a clear icon that says view translated message in the native user's language." |
| 34 | 2026-09-22 | **Sent messages are stored (`store/<slug>.sent.jsonl`, keyed by outgoing text)** so a message echoed back from WhatsApp history renders as Jason's original English, not the Russian that's actually in WhatsApp. Pairs with the id-keyed incoming translation cache (decision earlier this session). | Local-file conversation history so nothing re-translates and Jason's own words persist as English across reloads/restarts. DB later. |

| 35 | 2026-09-22 | **Photo sending: 📎 attach + Ctrl/⌘-V paste.** Bridge gains `sendMedia` (whatsapp-web.js `MessageMedia`); C# `/api/photo` saves the upload to `bridge/outgoing/`, sends it, and translates a caption only if one is typed. Pasting an image posts it straight through with no translation (pure pass-through). | Jason: "can I send him a photo" + "can I copy paste images… post over without translation." |
| 36 | 2026-09-22 | **The translator never refuses.** ProcessAsync prompt hardened: never pause, never ask, never return prose; transliterate unfamiliar names and always emit JSON. A message failed to send because the model paused to ask who an unfamiliar first name was — a translator blocking a send over an unknown name is a bug. | A real message hard-failed with "Model did not return JSON" when it hit a name not in the people network. Unknown names must transliterate and send. |

| 37 | 2026-09-22 | **Second library patch for media send** (`patches/wwebjs-media-fix.diff`, from wwebjs #201923): drop the media model's private `__x_id` before it's spread into the outgoing Msg, or current WhatsApp Web throws "Data passed to getter must include an id property" on every photo. Re-apply after `npm install` alongside the getChats patch. | Jason's first photo send hit this exact error; it's the same WA-Web-2.3000.x internal-rename class as the getChats bug. Text was never affected. |

| 38 | 2026-09-22 | **Open-sourced as a public template** (MIT). Repo root is the project; a `.gitignore` excludes ALL private/runtime data (`.wwebjs_auth/`, `store/`, `logs/`, `people/`, `media/`, `outgoing/`, `copper.json`, `node_modules`, `bin`/`obj`). Ships `copper.example.json`, `people.example/`, README, LICENSE, and the two patches. Real family data never enters git history. | Jason: clean up, document, publish to share with a friend in Kazakhstan. A public repo of a family-messaging tool must carry zero real PII. |
| 39 | 2026-09-22 | **Planned onboarding (roadmap):** each user authenticates their own WhatsApp (QR), then imports contacts by *choosing conversations* — each selected chat becomes a bridged contact with a people file seeded from its history. Replaces hand-editing `copper.json`; implies multi-contact support. | Jason: "there should be a phase where we import people from WhatsApp… have them auth and then just allow them to import by choosing conversations." |

| 40 | 2026-09-22 | **Automatic language seeding (required feature).** When an unknown person first speaks, the engine auto-detects their language (returned by the translate edge, no extra call), auto-creates their `people/<slug>.md` with a grounding stub (detected language + identity + first-contact date), and uses it thereafter. No manual setup — it reuses the existing auto-learn/grounding machinery; the only new piece is "unknown sender → create file + record language." Targeted for the Telegram multi-chat wiring; applies to any transport. | Jason: "this must be a feature automatically." Manual per-person setup would break the invisible-bridge promise, especially for group chats where people arrive continuously. |

| 41 | 2026-09-22 | **Telegram is the go-forward transport; the WhatsApp/web-UI build is the proof-of-concept.** New product work targets the Telegram bot. WhatsApp code stays in the repo but isn't the focus. | Jason: "I want to use telegram moving forward." Telegram is official/sanctioned, no ban risk, no patches, has native payments — the commercial path (decision #39-adjacent). |
| 42 | 2026-09-22 | **CI via GitHub Actions** (`.github/workflows/ci.yml`): build the C# backend + syntax-check both bridges on every push and every PR to `master`. Deploy is separate and needs a host + `ANTHROPIC_API_KEY` (a deployed bot can't use a personal Claude CLI login). | Jason wants a Git action to deploy the bot. CI is the safe, immediate foundation; deploy is gated on choosing a host and productionizing the brain to an API key. |

| 43 | 2026-09-22 | **Deploy target: GCP Cloud Run** (AWS App Runner the equivalent fallback), bot via **webhook**, deployed by a GitHub Action on merge to master. Requires collapsing the laptop build into ONE C# service: drop the Node bridge (call Telegram HTTPS directly), add a webhook endpoint, replace the Claude CLI with the Anthropic API (`ANTHROPIC_API_KEY`), and move state to a bucket/DB (or run ephemeral, letting auto-seed rebuild). Engine/prompts/people-logic all carry over. `deploy.yml` scaffolded but inert (workflow_dispatch + `DEPLOY_ENABLED` gate) until the refactor + secrets exist. | Jason: "use telegram moving forward… an action in Git to deploy… I can use aws or GCP." The two-process + CLI + local-files shape can't run in the cloud; the refactor is also a real simplification. |

| 44 | 2026-09-22 | **`copper-bot` built: single C# cloud service** (`bot/`). Talks to Telegram + Anthropic directly over HTTPS; webhook in cloud, long-poll locally (same binary, chosen by `PUBLIC_URL`). MVP: text-only, two-language group translation with people-network context + auto-seed, one Haiku call per message, state on local disk. Model/langs via env. Dockerfile + Cloud Run deploy wired. | The deployable form of decisions #41–43. Reuses the engine's ideas, sheds the laptop-era Node bridge + CLI + web UI. Media, N-language fan-out, and bucket/DB state are documented follow-ups. |

| 45 | 2026-09-22 | **GCP provisioned + all secrets hooked up.** Project `copper-babel-bot` (num 48720416559), billing linked; APIs on (Vertex AI, Cloud Run, Artifact Registry, Cloud Build, Secret Manager). Deploy SA `copper-deploy` (run.admin/artifactregistry.admin/cloudbuild.builds.editor/storage.admin/iam.serviceAccountUser); its key → GitHub secrets `GCP_SA_KEY`/`GCP_PROJECT`/`GCP_PROJECT_NUMBER` (key deleted from disk after upload). Runtime SA (default compute) granted aiplatform.user + secretmanager.secretAccessor. Telegram token in Secret Manager. Model = Claude on Vertex (no API key). **Manual gate: Claude must be enabled once in the project's Vertex Model Garden (accept Anthropic terms) — can't be done via CLI.** | Jason: use a model in GCP, new project, hook up all secrets. Vertex keeps auth+billing GCP-native. | 

| 46 | 2026-09-22 | **Model switched to Gemini 2.5 Flash on Vertex AI** (supersedes the Claude choices in #5/#20/#44/#45). Claude on Vertex is a partner model: a new project has **zero quota** until a manual increase is approved, and enabling it requires clicking through Anthropic's terms in Model Garden. Gemini is Google's own model — immediate quota on a new project, billed straight through GCP (no API key), cheapest option, and it translates faithfully with the people-context prompt + thinking disabled. Verified live from the deployed container. Claude Haiku 4.5 is also enabled and remains a one-line `COPPER_MODEL` switch. | Getting it running end to end without a quota-approval wait; Jason funds it himself so cheapest-that-holds-quality wins; keep the model swappable. |
| 47 | 2026-09-22 | **Which earlier decisions are now historical.** The go-forward product is the single-service Telegram bot (`bot/`) on Cloud Run with Gemini on Vertex (#41–#46). Decisions **#2–#37** describe the retired WhatsApp/web-UI proof-of-concept (Node `wa.js` bridge, `claude` CLI brain, browser/console send-gate, per-command processes, the WhatsApp media flow) — they are **historical context, not current architecture**. What carries forward: the VALUES (#18/#19), and the people-network / auto-seed / grounding / dignity ideas (#15–#17, #28, #40). What does not: the transport and plumbing specifics. Read #38+ for the current shape. | Architectural review (2026-09-22) found the log leaves a future reader with the wrong mental model; this row is the map. |

| 48 | 2026-09-22 | **Hardening sprint (agent-team review → fixes, verified in prod).** Webhook now authenticates Telegram's secret-token header (`WEBHOOK_SECRET` in Secret Manager) — 401 unsigned / 200 signed, verified. Personas moved from ephemeral Cloud Run disk to **Firestore** (survive scale-to-zero), scoped per chat (no cross-chat persona leak) — verified a persona persisted. Handler now awaits (Cloud Run CPU throttle), guards all fields + always acks 200, skips other bots, registers the webhook off the startup path, and logs PII-free error types. Docs reconciled to Gemini/Vertex/Firestore. | Two agent reviews (architecture + security) surfaced the "seen-coming" gaps: unauthenticated webhook and ephemeral persona memory (the core value). Both fixed and deployed as revision copper-bot-00002. |
| 49 | 2026-09-22 | **Repo clarity via labeling, not (yet) physical relocation.** Root README now leads with `bot/` as the product and marks `copper/`, `wa.js`, `telegram/` as historical. The physical archive into a `legacy/` tree (recommended by the review) is **deferred**: `wa.js`/`copper/` are a still-working on-disk WhatsApp runtime with a live `.wwebjs_auth` session, and the personal `whatsapp-family` skill hardcodes `C:\dev\marat` paths — moving them mid-stream would break both. Do the physical archive as a focused pass that also updates the skill's paths. `DECISIONS.md`/`VALUES.md`/`SPEC.md` stay in `copper/` (many links + memory point there); README links them. | Reduce future-session confusion now without breaking a functioning legacy setup or the skill. |

| 50 | 2026-09-23 | **Chat allowlist deferred — bot is intentionally allow-all for now.** `COPPER_ALLOWED_CHATS` stays empty (= serve any chat) so testers can use the bot freely. This is a conscious risk acceptance (the security review's C1 chat-allowlist is a *defense-in-depth* item; the webhook secret-token auth is the real access control and is in place). The pipeline reads the allowlist from the GitHub repo variable `COPPER_ALLOWED_CHATS`, and the bot logs each incoming chat id/type — so restricting later is just: set the variable, redeploy. Token intentionally NOT regenerated per Jason. Also: deploy workflow now only triggers on `bot/**` or workflow changes (doc commits no longer cause deploys). | Jason: keep it empty/allow-all for open testing; don't regen the token. |

Add new rows at the bottom with date + why. To reverse one, add a new row superseding it — don't edit history.
